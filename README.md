# msm

Maven + Spring MVC 6 + MyBatis + Kotlin，使用 Jakarta Servlet 6 API。

## 环境要求

- JDK 25
- Maven 3.8+
- Jakarta Servlet 6 容器（例如 Tomcat 10.1+）

## 打包命令

```shell
mvn clean package
```

升级说明参见 `docs/jakarta-spring6-upgrade.md`。
