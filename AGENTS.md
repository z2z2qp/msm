# AGENTS

本文件是仓库内所有模型与代理的唯一执行规范。

## 1. 原则与优先级

- 安全、边界和需确认事项优先，不得为迎合用户放弃正确判断。
- 用户明确指示决定任务目标；执行仍须遵守范围、提交、文档、编码保护和高风险确认要求。
- 信息不足时先说明假设、歧义和取舍；不足以支撑实施时不得猜测需求。
- 优先采用满足当前需求的最简单方案，不做推测性功能、过度抽象或无关重构。

## 2. 任务执行

- 进入仓库后先阅读根目录 `AGENTS.md`。
- 只执行用户明确指定的任务，并严格限制在当前对象和 `scope` 内；跨范围修改须先获得明确授权。
- 多任务按用户给定顺序逐个完成“实现、验证、提交并推送”闭环；若存在明确依赖风险，先说明再调整顺序。
- 修改现有代码时保持原有风格和编码，只改实现需求所必需的内容。
- 只清理由本轮修改产生的无用导入、变量、函数和文件；发现无关问题仅报告，不顺手处理。

## 3. 实施与验证

实施前将需求转为可验证目标。多步骤任务使用以下简要格式：

```text
1. [步骤] -> verify: [检查项]
2. [步骤] -> verify: [检查项]
```

- 每项实际改动必须执行与风险相匹配的验证；无可信验证入口时必须明确说明，不得宣称已可用。
- 用户未明确要求时，不得编写或执行 Playwright 端到端浏览器测试。
- 禁止全量格式化、全量转码和批量无差别替换。
- 本系统仅按桌面端设计；默认不实施移动端适配、移动端断点或移动端专项验证，除非用户另行明确要求。
- 修改核心协议、内存地址或数据库结构前，必须获得用户明确批准。
- 修改网络包处理、线程并发、鉴权或反作弊路径前，必须获得用户确认。

## 4. 本地调试与启动

- 本地调试必须直接在宿主机使用热更新模式启动，不得以普通生产启动模式替代调试模式。
- 若现有项目不支持本地热更新，先说明缺口并取得用户授权后再修改相关配置。

## 5. 提交规范

- 功能提交信息统一使用 `feat: 实现xxx功能` 格式，其中 `xxx` 必须准确描述本次实现的业务功能。
- 一次提交只包含同一任务范围内的改动，不混入无关格式化、重构或清理。
- 提交前必须先拉取当前分支的远端最新代码；确认拉取成功且无冲突后，方可提交并推送。
- 每个任务完成且验证通过后，自动提交当前任务范围内的改动，并推送当前分支。
- 提交或推送失败时立即报告原因，不得通过合并、变基、强制推送或改写历史自行处理。

## 6. 文档

- 根目录必须保留 `docs/`；影响后续使用、部署、验证或协作的行为、配置、依赖变更，必须同步更新其中的文档。
- 纯问答、只读检查、配置解释和方案讨论不强制写入文件。

## 7. 工具与协作

- 选择完成当前任务的最小有效工具路径。
- `desktop-commander` 仅用于跨盘、非工作区文件整理或 PDF/Word/Excel 等本机文档处理。
- `Serena` 仅用于符号、引用、调用关系、结构概览和精确重命名等语义级代码理解。
- 多智能体并行修改前必须划分独立文件或模块边界；同一文件默认串行处理。

## 8. 回复方式

- 先给明确业务结论，再说明影响结论的原因、当前风险和最小下一步。
- 默认使用以下结构；简单问答可直接短答，但仍须先给结论：

```text
业务结论
原因
风险
下一步
```

- 默认面向业务或项目负责人，优先说明能否推进、卡点、继续执行的影响和最小动作。
- 用户未要求源码细节时，不以函数名、调用链、文件和行号作为主要表达；必要技术依据统一置于末尾的“技术依据（按需阅读）”。
